import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { UsersToFollowComponent } from './users-to-follow.component';

describe('UsersToFollowComponent', () => {
  let component: UsersToFollowComponent;
  let fixture: ComponentFixture<UsersToFollowComponent>;

 /*  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ UsersToFollowComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(UsersToFollowComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
