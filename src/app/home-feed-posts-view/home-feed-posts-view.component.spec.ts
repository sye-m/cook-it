import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { HomeFeedPostsViewComponent } from './home-feed-posts-view.component';

describe('HomeFeedPostsViewComponent', () => {
  let component: HomeFeedPostsViewComponent;
  let fixture: ComponentFixture<HomeFeedPostsViewComponent>;

  /* beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ HomeFeedPostsViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(HomeFeedPostsViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
